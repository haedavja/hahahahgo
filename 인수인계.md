# 프로젝트 인수인계 문서

## 프로젝트 개요
- **프로젝트명**: hahahahgo (턴제 카드 배틀 게임)
- **경로**: `/home/user/hahahahgo` (또는 `Z:\바이브코딩\memory bank\hahahahgo`)
- **기술 스택**: React 18, TypeScript, Zustand (상태관리), Vite, Tailwind CSS
- **현재 브랜치**: `claude/merge-latest-commits-yT3iK`
- **최신 커밋**: `89206797` (2026-01-09 기준)

---

## 2026-01-09 최근 완료된 작업

### 1. 카드 희귀도 시스템 구현 (커밋: `0e05c946`)

**변경된 파일:**
- `src/components/battle/battleData.ts` - 모든 카드에 rarity 필드 추가
- `src/components/battle/hooks/useRewardSelection.ts` - 가중치 기반 카드 선택 함수 추가
- `src/components/battle/ui/CardRewardModal.tsx` - 희귀도 배지 및 시각 효과

**희귀도 등급 및 드랍 확률:**
| 등급 | rarity | 드랍 확률 |
|------|--------|----------|
| 일반 | common | 60% |
| 희귀 | rare | 30% |
| 특별 | special | 8% |
| 전설 | legendary | 2% |

**가중치 기반 선택 함수** (`useRewardSelection.ts:35-67`):
```typescript
function selectCardsByRarity(cards: Card[], count: number): Card[]
```

---

### 2. 특성 보상 시스템 개선 (커밋: `8bd0d2fa`, `334bd4ef`)

**변경된 파일:** `src/components/battle/hooks/useRewardSelection.ts`

**변경 사항:**
- 특성 보상은 **엘리트/보스/이벤트/던전 전투에서만** 획득 가능
- 특성 보상 확률: **100%** (이전 30%)

**코드 위치 및 핵심 로직** (`useRewardSelection.ts:190-216`):
```typescript
const TRAIT_REWARD_BATTLE_TYPES = ['elite', 'boss', 'event', 'dungeon'];
const TRAIT_REWARD_CHANCE = 1.0; // 100%

const showCardRewardModal = useCallback(() => {
  const activeBattle = useGameStore.getState().activeBattle;
  const battleKind = activeBattle?.kind || '';
  const canGetTraitReward = TRAIT_REWARD_BATTLE_TYPES.includes(battleKind);

  if (canGetTraitReward && Math.random() < TRAIT_REWARD_CHANCE) {
    // 특성 보상 로직...
  }
}, []);
```

---

### 3. 이벤트 특성(trait) 보상 처리 추가 (커밋: `89206797`)

**문제:** 이벤트 선택지에서 `rewards.trait: "random"` 설정이 작동하지 않음
  - 예: "성찰" 이벤트의 "뛰어든다" 선택지에서 특성 보상이 0으로 표시

**원인:** `eventSlice.ts`에서 `rewards.card`만 처리하고 `rewards.trait`는 처리하지 않음

**변경된 파일:** `src/state/slices/eventSlice.ts`

**추가된 로직** (`eventSlice.ts:96-116`):
```typescript
// 특성 보상 처리
let newStoredTraits = [...(state.storedTraits || [])];
if (choice.rewards?.trait) {
  const traitReward = choice.rewards.trait;
  if (traitReward === 'random') {
    // 랜덤 특성 선택 (긍정 특성만, weight 2 이하, 중복 방지)
    const availableTraits = Object.values(TRAITS).filter(
      (t) => t.type === 'positive' && t.weight <= 2 && !newStoredTraits.includes(t.id)
    );
    if (availableTraits.length > 0) {
      const randomIndex = Math.floor(Math.random() * availableTraits.length);
      const selectedTrait = availableTraits[randomIndex];
      newStoredTraits.push(selectedTrait.id);
    }
  } else if (typeof traitReward === 'string') {
    // 특정 특성 ID가 지정된 경우
    if (!newStoredTraits.includes(traitReward)) {
      newStoredTraits.push(traitReward);
    }
  }
}
```

---

## 이전 완료된 작업 (2026-01-08 이전)

### 1. 타임라인 속도 최적화 (커밋됨)

사용자 요청: "스피드한 게임감을 원해" - 타임라인 카드 진행이 너무 느림

**최적화 결과: 전체 55% 속도 향상**

#### 변경된 파일 및 값

**`src/components/battle/LegacyBattleApp.jsx`**

| 위치 (라인) | 항목 | 이전 값 | 현재 값 | 설명 |
|-------------|------|---------|---------|------|
| ~1584 | 자동진행 딜레이 | 1000ms | **450ms** | useEffect 타이머, 다음 카드로 넘어가는 대기 시간 |
| ~1592 | 에테르 계산 애니메이션 | 900ms | **400ms** | 턴 종료 후 에테르 정산 시작 딜레이 |
| ~1431 | 카드 실행 메인 딜레이 | 500ms | **250ms** | stepOnce 내 setTimeout, 시곗바늘 이동 후 카드 발동 |
| ~1400 | 카드 흔들림 애니메이션 | 350ms | **200ms** | 카드 실행 중 흔들림 효과 지속 시간 |
| ~1406 | 마지막 카드 페이드아웃 | 300ms | **150ms** | 타임라인 인디케이터 숨김 타이밍 |
| ~1423 | 카드 소멸 시작 딜레이 | 300ms | **150ms** | 플레이어 카드 사라지기 시작 |
| ~1426 | 카드 소멸 지속 시간 | 600ms | **300ms** | 카드 완전히 사라지는 애니메이션 |

**`src/components/battle/ui/TimelineDisplay.jsx`**

| 위치 (라인 90) | 항목 | 이전 값 | 현재 값 | 설명 |
|----------------|------|---------|---------|------|
| transition | 시곗바늘 이동 속도 | 0.5s | **0.25s** | CSS transition, 카드 실행과 동기화 필수 |

#### 타이밍 동기화 원리
```
시곗바늘 이동 (0.25s) → 카드 발동 대기 (250ms) → 흔들림 (200ms) → 다음 카드 (450ms)
```
- 시곗바늘 transition(0.25s)과 카드 발동 딜레이(250ms)가 일치해야 "시곗바늘이 카드에 도달하면 발동"되는 느낌
- 자동진행 딜레이(450ms)는 반드시 stepOnce 총 실행 시간(250ms + 200ms = 450ms)보다 길거나 같아야 함

#### 중요 버그 히스토리
자동진행 딜레이를 400ms로 설정했을 때 발생한 버그:
- **증상**: "타임라인에서는 플레이어 카드만 발동되는데 실제로는 몬스터 카드만 실행됨"
- **원인**: stepOnce 실행(500ms)이 완료되기 전에 다음 stepOnce 호출
- **해결**: 자동진행 딜레이를 600ms → 450ms (stepOnce 최적화 후)로 조정

---

### 2. 상징 시각적 피드백 개선 (커밋됨)

사용자 요청: "패시브 상징 색을 하늘색으로, 덜 밝게 해줘"

**파일**: `src/components/battle/ui/RelicDisplay.jsx`

#### 상징 하이라이트 분류
```javascript
// 패시브 (상시 발동) - isPersistent
const isPersistent = relic.effects?.type === 'PASSIVE'
  || relic.effects?.type === 'ON_TURN_START'
  || relicId === 'bloodShackles';

// 일시 발동 - isActivated
const isActivated = activeEffects.some(e => e.relicId === relicId);
```

#### 색상/밝기 설정값

| 구분 | 색상 RGB | 밝기 (brightness) | 글로우 (glowIntensity) | 배경 투명도 (bgOpacity) | 테두리 투명도 (borderOpacity) | 그림자 강도 (shadowIntensity) |
|------|----------|-------------------|------------------------|-------------------------|-------------------------------|-------------------------------|
| **패시브** | (135, 206, 235) 하늘색 | 1.05 | 0.15 | 0.06 | 0.25 | 0.25 |
| **일시 발동** | (251, 191, 36) 황금색 | 1.15 | 0.35 | 0.12 | 0.5 | 0.5 |

#### 적용 CSS (동적 생성)
```javascript
filter: `brightness(${brightness}) drop-shadow(0 0 4px rgba(${r}, ${g}, ${b}, ${glowIntensity}))`
background: `rgba(${r}, ${g}, ${b}, ${bgOpacity})`
border: `1px solid rgba(${r}, ${g}, ${b}, ${borderOpacity})`
boxShadow: `0 0 15px rgba(${r}, ${g}, ${b}, ${shadowIntensity})`
```

---

## 미커밋 변경사항 (작업 진행 중)

### 수정된 파일

| 파일 | 주요 변경 내용 |
|------|---------------|
| `# 상태이상.MD` | 상태이상 이름 변경 (대공세→공세+, 대수세→수세+ 등) |
| `combatActions.js` | 취약(vulnMult) 시스템 제거 - 차단 성공 시 취약 부여 로직 삭제 |
| `battleSimulation.js` | 취약 시스템 제거 동기화 |
| `HandArea.jsx` | 카드 아이콘 fallback 추가 (Sword/Shield) |
| `PlayerHpBar.jsx` | 통찰 레벨 시스템 (-3~+3), 상태 툴팁 추가 |
| `DevTools.jsx` | 개발자 도구 이변 관련 기능 |
| `MapDemo.jsx` | 맵 데모 변경 |
| `gameStore.js` | devForcedAnomalies, mapRisk 상태 추가 |
| `useGameState.js` | 상태 훅 변경 |
| `events.js` | **삭제됨** - newEvents.js로 대체 |

### 신규 파일 (untracked)

| 파일 | 설명 |
|------|------|
| `src/components/battle/ui/AnomalyDisplay.jsx` | 이변 표시 UI 컴포넌트 |
| `src/data/anomalies.js` | 이변 데이터 정의 |
| `src/data/newEvents.js` | 새 이벤트 시스템 데이터 |
| `src/lib/anomalyUtils.js` | selectBattleAnomalies, applyAnomalyEffects 함수 |
| `이벤트.MD` | 이벤트 기획 문서 |

---

## 시스템 아키텍처

### 전투 흐름 (LegacyBattleApp.jsx)

```
[select] 카드 선택 → [respond] 대응 → [resolve] 타임라인 진행 → [턴 종료]
                                              ↓
                                    stepOnce() 반복 호출
                                              ↓
                                    시곗바늘 이동 (250ms)
                                              ↓
                                    카드 발동 + 흔들림 (200ms)
                                              ↓
                                    executeCardAction()
                                              ↓
                                    자동진행 대기 (450ms)
                                              ↓
                                    다음 카드로...
```

### 통찰 레벨 시스템

**파일**: `PlayerHpBar.jsx`, `LegacyBattleApp.jsx`

```javascript
// 계산: insight - shroud - insightPenalty (범위: -3 ~ +3)
const insightLevel = Math.max(-3, Math.min(3, insight - shroud - insightPenalty));
```

| 레벨 | 이름 | 이모지 | 색상 | 효과 |
|------|------|--------|------|------|
| -3 | 망각 | 🌑 | #1e293b | 타임라인, 적 체력/에테르 확인 불가 |
| -2 | 미련 | 🌘 | #475569 | 진행단계에서 적 타임라인 확인 불가 |
| -1 | 우둔 | 🌫️ | #94a3b8 | 대응단계에서 적 타임라인 확인 불가 |
| 0 | 평온 | 🌕 | #e2e8f0 | 선택단계에서 적 카드 3개 확인 (정보 없음) |
| +1 | 예측 | 🔮 | #c4b5fd | 선택단계에서 적 카드 2개 확인 |
| +2 | 독심 | 👁️ | #a78bfa | 선택단계에서 적 카드 모두 확인 |
| +3 | 혜안 | ✨ | #8b5cf6 | 적 카드 모두 + 카드 정보 확인 |

### 이변 시스템 (Anomaly)

**파일**: `LegacyBattleApp.jsx`, `anomalyUtils.js`, `anomalies.js`

```javascript
// 전투 시작 시 이변 선택
const selectedAnomalies = selectBattleAnomalies(mapRisk, isBoss, devForcedAnomalies);

// 플레이어에게 이변 효과 적용
const playerWithAnomalies = applyAnomalyEffects(selectedAnomalies, player, gameState);
```

**적용되는 패널티들:**
- `energyPenalty`: 최대 행동력 감소
- `speedPenalty`: 최대 속도 감소
- `drawPenalty`: 드로우 감소
- `insightPenalty`: 통찰 레벨 감소
- `etherBan`: 에테르 획득 불가

---

## 파일 구조

```
src/
├── components/
│   ├── battle/
│   │   ├── LegacyBattleApp.jsx     # 메인 전투 로직
│   │   ├── logic/
│   │   │   └── combatActions.js    # 데미지 계산, 카드 효과
│   │   ├── ui/
│   │   │   ├── TimelineDisplay.jsx # 타임라인 UI, 시곗바늘
│   │   │   ├── RelicDisplay.jsx    # 상징 표시
│   │   │   ├── HandArea.jsx        # 손패 카드
│   │   │   ├── PlayerHpBar.jsx     # HP바, 상태 표시
│   │   │   ├── AnomalyDisplay.jsx  # 이변 UI (신규)
│   │   │   └── ...
│   │   └── utils/
│   │       ├── battleUtils.js      # 전투 유틸리티
│   │       ├── battleSimulation.js # 전투 시뮬레이션
│   │       └── comboDetection.js   # 콤보 감지
│   ├── dev/
│   │   └── DevTools.jsx            # 개발자 도구
│   └── map/
│       └── MapDemo.jsx             # 맵 데모
├── data/
│   ├── anomalies.js                # 이변 데이터 (신규)
│   └── newEvents.js                # 이벤트 데이터 (신규)
├── lib/
│   └── anomalyUtils.js             # 이변 유틸리티 (신규)
└── state/
    ├── gameStore.js                # Zustand 스토어
    └── useGameState.js             # 상태 훅
```

---

## Git 상태

```bash
# 현재 상태
브랜치: claude/check-responsiveness-01WbgS136iR7Qv8ZmsANG367
로컬이 origin보다 117 커밋 앞서있음

# 최근 커밋
7ffcd9e perf: 타임라인 진행 속도 최적화 및 상징 시각적 피드백 개선
0738038 feat: 토큰 시스템 구현 및 전투 로직 통합
a2c8915 Refactor: Extract TraitBadge and CardStatsSidebar components
```

---

## 빌드 및 실행

```bash
cd "Z:\바이브코딩\memory bank\hahahahgo"
npm install
npm run dev
```

---

## 주의사항 및 알려진 이슈

1. **타이밍 동기화 필수**: 시곗바늘 transition(TimelineDisplay.jsx)과 카드 발동 딜레이(LegacyBattleApp.jsx)는 반드시 일치해야 함

2. **자동진행 딜레이 하한선**: 450ms 미만으로 줄이면 카드 실행 버그 발생 가능

3. **취약 시스템 제거됨**: vulnMult 관련 코드가 combatActions.js에서 제거됨 (미커밋)

4. **CRLF 경고**: Windows 환경 LF→CRLF 변환 경고 (기능 영향 없음)

---

## 사용자 요청 히스토리 (한국어)

1. "전투에서 상시 발동하는 상징들의 발동할때 색을 좀 옅은 하늘색으로 바꾸고 싶어"
2. "패시브 타입 상징 말고 다른 상징들은 기존의 색 그대로 유지하게해"
3. "패시브 타입 상징들의 밝기 좀 덜 밝게해봐. 눈에 잘 안띄게"
4. "타임라인에서 다음 카드로 넘어가는데 걸리는 딜레이가 좀 긴데 많이 줄여봐. 스피드한 게임감을 원해"
5. "카드들이 제대로 발동안돼. 타임라인상에서는 내 카드만 발동되는데 실제로는 몬스터측 카드만 발동돼"
6. "이제 제대로 되긴 하는데. 나는 이게 너무 느리다고 봐. 어떤 방법이 없을까?"
7. "빠른데 시곗바늘보다 빠르게 발동하네 함 맞춰봐"

---

**최종 업데이트**: 2026-01-09
**작성자**: Claude (Anthropic AI)

---

## 미해결 작업 / 향후 고려사항

### 1. 상징(relic) 보상 검토 필요
- 이벤트에서 `rewards.relic` 보상이 있는 경우 처리 로직 확인 필요
- `eventSlice.ts`에서 `rewards.trait`처럼 `rewards.relic` 처리가 누락되었을 수 있음

### 2. 전투 타입 (`activeBattle.kind`) 값 목록
| 값 | 설명 |
|------|------|
| `battle` | 일반 전투 |
| `elite` | 엘리트 전투 |
| `boss` | 보스 전투 |
| `dungeon` | 던전 전투 |
| `event` | 이벤트 전투 |

### 3. 특성 보상 조건 (`TRAITS`)
- `type === 'positive'` (긍정 특성만)
- `weight <= 2` (낮은 가중치만)
- 중복 방지 (이미 가진 특성 제외)
