# v11-16-10:51 - HP 글자 크기 통일, 손패 인터페이스 축소 및 최적화

**날짜**: 2025-11-16 10:51 (KST)
**커밋**: (커밋 예정)

## 변경 사항

### 1. HP 글자 크기 통일

**파일**: `src/components/battle/LegacyBattleApp.jsx` (424-429줄)

#### 변경 전
```jsx
<div style={{fontSize: '16px', fontWeight: 'bold', color: '#e2e8f0', marginBottom: '8px'}}>
  플레이어 HP {player.hp} → {res.finalPHp}
</div>
<div style={{fontSize: '14px', color: '#fca5a5'}}>
  몬스터 HP {enemy.hp} → {res.finalEHp}
</div>
```

#### 변경 후
```jsx
<div style={{fontSize: '16px', fontWeight: 'bold', color: '#e2e8f0', marginBottom: '8px'}}>
  플레이어 HP {player.hp} → {res.finalPHp}
</div>
<div style={{fontSize: '16px', fontWeight: 'bold', color: '#fca5a5'}}>
  몬스터 HP {enemy.hp} → {res.finalEHp}
</div>
```

- 몬스터 HP 글자 크기: 14px → 16px
- 몬스터 HP도 굵게 표시: fontWeight: 'bold' 추가

### 2. 손패 영역 크기 축소

**파일**: `src/components/battle/legacy-battle.css`

#### .hand-area (524-538줄)

**변경 전**:
```css
.hand-area {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  width: 100%;
  max-width: 1400px;
  margin: 0 auto;
  background: rgba(4, 6, 22, 0.75);
  backdrop-filter: blur(8px);
  border-radius: 28px 28px 0 0;
  padding: 28px 32px;
  ...
}
```

**변경 후**:
```css
.hand-area {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 340px;
  width: auto;
  background: rgba(4, 6, 22, 0.5);
  backdrop-filter: blur(8px);
  border-radius: 16px 16px 0 0;
  padding: 12px 16px;
  ...
}
```

**주요 변경**:
- **right: 0 → 340px**: 예상 피해량 창(320px)과 안 겹치게 왼쪽으로 이동
- **width: 100% → auto**, **max-width 제거**: 유연한 너비
- **background: 0.75 → 0.5**: 더 투명하게 (25% → 50% 투명)
- **border-radius: 28px → 16px**: 더 컴팩트한 모서리
- **padding: 28px 32px → 12px 16px**: 절반 이하로 축소
- **margin: 0 auto 제거**: 왼쪽 정렬

### 3. 손패 헤더 축소

**파일**: `src/components/battle/legacy-battle.css`

#### .hand-area-header (540-546줄)

**변경 전**:
```css
.hand-area-header {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 20px;
  align-items: center;
}
```

**변경 후**:
```css
.hand-area-header {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 8px;
  align-items: center;
  margin-bottom: 8px;
}
```

- **gap: 20px → 8px**: 열 간격 축소
- **margin-bottom: 8px 추가**: 카드와 헤더 간격 최소화

#### .hand-heading (548-552줄)

**변경 전**:
```css
.hand-heading {
  display: flex;
  align-items: center;
  gap: 18px;
}
```

**변경 후**:
```css
.hand-heading {
  display: flex;
  align-items: center;
  gap: 8px;
}
```

- **gap: 18px → 8px**: 제목과 아이콘 간격 축소

#### .hand-title (554-558줄)

**변경 전**:
```css
.hand-title {
  font-size: 2.75rem;
  font-weight: 900;
  color: #f8fafc;
}
```

**변경 후**:
```css
.hand-title {
  font-size: 1.5rem;
  font-weight: 900;
  color: #f8fafc;
}
```

- **font-size: 2.75rem → 1.5rem**: "손패" 텍스트 크기 대폭 축소 (약 45% 축소)

### 4. 카드 영역 축소

**파일**: `src/components/battle/legacy-battle.css`

#### .hand-cards (504-509줄)

**변경 전**:
```css
.hand-cards {
  display: flex;
  gap: 18px;
  padding: 24px 32px;
  overflow-x: auto;
}
```

**변경 후**:
```css
.hand-cards {
  display: flex;
  gap: 12px;
  padding: 8px 0;
  overflow-x: auto;
}
```

- **gap: 18px → 12px**: 카드 간격 축소
- **padding: 24px 32px → 8px 0**: 상하 패딩만 최소화, 좌우 패딩 제거

#### .hand-combo (580-585줄)

**변경 전**:
```css
.hand-combo {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 56px;
}
```

**변경 후**:
```css
.hand-combo {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 40px;
}
```

- **min-height: 56px → 40px**: 콤보 영역 높이 축소

## 기술 세부사항

### 크기 비교

| 요소 | 이전 | 변경 후 | 감소율 |
|------|------|---------|--------|
| 손패 padding | 28px 32px | 12px 16px | ~57% |
| 손패 제목 | 2.75rem | 1.5rem | 45% |
| 카드 간격 | 18px | 12px | 33% |
| 카드 padding | 24px 32px | 8px 0 | ~75% |
| 헤더 gap | 20px | 8px | 60% |
| 제목 gap | 18px | 8px | 56% |
| 콤보 높이 | 56px | 40px | 29% |

### 투명도 변경

| 요소 | 이전 | 변경 후 | 효과 |
|------|------|---------|------|
| background alpha | 0.75 | 0.5 | 25% → 50% 투명 |

### 위치 조정

**이전**:
```
┌────────────────────────────────────┐
│  손패 (전체 너비, 중앙 정렬)       │
└────────────────────────────────────┘
```

**변경 후**:
```
┌──────────────────────────┐┃예상 피해량┃
│  손패 (왼쪽 정렬)        │┃  창(340px)┃
└──────────────────────────┘┃          ┃
```

- 손패가 예상 피해량 창(320px + 마진)과 안 겹치게 right: 340px 설정
- 왼쪽부터 채워지는 레이아웃

## UI 개선 효과

### 장점

1. **화면 공간 확보**
   - 손패가 차지하는 공간이 절반 이하로 축소
   - 전투 화면이 더 잘 보임

2. **정보 밀도 향상**
   - 컴팩트한 레이아웃으로 더 많은 정보 표시
   - 텍스트와 버튼이 카드에 바짝 붙어 공간 효율 증가

3. **투명도 증가**
   - 손패가 더 투명해져서 뒤 배경이 더 잘 보임
   - 전투 상황 파악 용이

4. **예상 피해량 창과 분리**
   - 손패가 왼쪽으로 이동하여 예상 피해량 창과 안 겹침
   - 두 영역 모두 독립적으로 사용 가능

5. **HP 정보 일관성**
   - 플레이어 HP와 몬스터 HP가 동일한 크기로 표시
   - 시각적 일관성 향상

### 사용자 경험

- **전투 집중**: 손패가 작아져서 전투 화면에 집중 가능
- **빠른 스캔**: 컴팩트한 레이아웃으로 정보 빠르게 파악
- **배경 확인**: 투명도 증가로 전투 상황 동시 확인 가능

## 이전 버전과의 차이

**이전 버전 (v11-16-10:46)**:
- 몬스터 HP: 14px
- 손패: 전체 너비, 중앙 정렬, 큰 패딩
- 손패 제목: 2.75rem (매우 큼)
- 손패 투명도: 75% 불투명 (25% 투명)

**현재 버전**:
- 몬스터 HP: 16px (플레이어와 동일)
- 손패: 왼쪽 정렬, right: 340px, 작은 패딩
- 손패 제목: 1.5rem (45% 축소)
- 손패 투명도: 50% 불투명 (50% 투명)

## 다음 AI를 위한 참고사항

1. **손패 위치 조정**:
   - right: 340px는 예상 피해량 창(320px) + 마진(20px)
   - 예상 피해량 창 너비 변경 시 함께 조정 필요

2. **투명도 밸런스**:
   - 현재 0.5 (50% 투명)
   - 너무 투명하면 가독성 저하 주의
   - 필요시 0.4 ~ 0.6 사이에서 조정

3. **패딩 최소값**:
   - 현재 12px 16px (상하 좌우)
   - 8px 이하로 줄이면 버튼 클릭 어려울 수 있음

4. **제목 크기**:
   - 현재 1.5rem
   - 1rem 이하로 줄이면 너무 작아질 수 있음

5. **카드 크기**:
   - 카드 자체 크기는 변경하지 않음
   - 카드 주변 간격만 축소
   - 카드 크기 변경 필요시 별도 작업 필요

6. **반응형**:
   - 작은 화면에서 right: 340px가 부담될 수 있음
   - 미디어 쿼리로 조정 고려

## 변경 파일 목록

- `src/components/battle/LegacyBattleApp.jsx`
  - 몬스터 HP 글자 크기 및 굵기 변경 (427-429줄)
- `src/components/battle/legacy-battle.css`
  - .hand-area 축소 및 위치 조정 (524-538줄)
  - .hand-area-header 간격 축소 (540-546줄)
  - .hand-heading 간격 축소 (548-552줄)
  - .hand-title 크기 축소 (554-558줄)
  - .hand-cards 패딩 및 간격 축소 (504-509줄)
  - .hand-combo 높이 축소 (580-585줄)
- `src/components/map/MapDemo.jsx`
  - 버전 태그 업데이트
- `CHANGELOG/v11-16-10-51.md`
  - 새 버전 문서 생성
