# v11-16-10:57 - 손패 완전 투명화, 카드 레이아웃 개선

**날짜**: 2025-11-16 10:57 (KST)
**커밋**: (커밋 예정)

## 변경 사항

### 1. 손패 영역 완전 투명화

**파일**: `src/components/battle/legacy-battle.css` (524-537줄)

#### 변경 전
```css
.hand-area {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 340px;
  width: auto;
  background: rgba(4, 6, 22, 0.5);
  backdrop-filter: blur(8px);
  border-radius: 16px 16px 0 0;
  padding: 12px 16px;
  border: 1px solid rgba(255, 255, 255, 0.08);
  box-shadow: 0 -10px 40px rgba(0, 0, 0, 0.6);
  z-index: 100;
}
```

#### 변경 후
```css
.hand-area {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 340px;
  width: auto;
  background: transparent;
  backdrop-filter: none;
  border-radius: 16px 16px 0 0;
  padding: 12px 16px;
  border: none;
  box-shadow: none;
  z-index: 100;
}
```

**주요 변경**:
- **background**: rgba(4, 6, 22, 0.5) → transparent (완전 투명)
- **backdrop-filter**: blur(8px) → none (블러 효과 제거)
- **border**: 1px solid → none (테두리 제거)
- **box-shadow**: 제거 (그림자 제거)

### 2. 카드 헤더 위치 조정

**파일**: `src/components/battle/legacy-battle.css` (465-472줄)

#### 변경 전
```css
.game-card-large .card-header {
  text-align: center;
  color: white;
  margin-top: 72px;
  font-size: 1rem;
  font-weight: 800;
  padding: 0 12px;
}
```

#### 변경 후
```css
.game-card-large .card-header {
  text-align: center;
  color: white;
  margin-top: 8px;
  font-size: 1rem;
  font-weight: 800;
  padding: 0 12px;
}
```

- **margin-top**: 72px → 8px (카드 이름을 위로 대폭 이동)
- 카드 상단 여백 89% 감소

### 3. 카드 아이콘 영역 개선

**파일**: `src/components/battle/legacy-battle.css` (474-481줄)

#### 변경 전
```css
.game-card-large .card-icon-area {
  margin-top: auto;
  display: flex;
  align-items: center;
  justify-content: center;
  padding-bottom: 16px;
}
```

#### 변경 후
```css
.game-card-large .card-icon-area {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 8px 0;
  min-height: 60px;
}
```

**주요 변경**:
- **margin-top: auto → flex: 1**: 남은 공간을 균등하게 차지
- **padding-bottom: 16px → padding: 8px 0**: 패딩 균형 조정
- **min-height: 60px 추가**: 최소 높이 보장

## 기술 세부사항

### 카드 레이아웃 구조 변경

**이전 (height: 220px)**:
```
┌────────────────┐
│ 💰 (16px)      │ ← card-cost-corner
│                │
│                │
│                │ ← 72px 여백
│  카드 이름     │ ← card-header
│                │
│                │
│                │
│      🗡️        │ ← card-icon-area (margin-top: auto)
│   ────────     │
│   공격력 정보  │ ← card-footer (잘림 위험)
└────────────────┘
```

**변경 후 (height: 220px)**:
```
┌────────────────┐
│ 💰 (16px)      │ ← card-cost-corner
│  카드 이름     │ ← card-header (8px 여백)
│                │
│                │
│      🗡️        │ ← card-icon-area (flex: 1)
│                │
│                │
│   ────────     │
│   공격력 정보  │ ← card-footer (잘 보임)
└────────────────┘
```

### 공간 분배

**이전**:
- card-cost-corner: 고정 (absolute, top: 16px)
- card-header: margin-top 72px
- card-icon-area: margin-top auto (남은 공간 모두 차지)
- card-footer: 하단 (잘릴 위험)

**변경 후**:
- card-cost-corner: 고정 (absolute, top: 16px)
- card-header: margin-top 8px (위로 올림)
- card-icon-area: flex: 1 (남은 공간 균등 차지)
- card-footer: 하단 (잘 보임)

### 투명도 비교

| 요소 | 이전 | 변경 후 |
|------|------|---------|
| background | rgba(4, 6, 22, 0.5) | transparent |
| backdrop-filter | blur(8px) | none |
| border | 1px solid | none |
| box-shadow | 있음 | none |

## UI 개선 효과

### 장점

1. **손패 완전 투명**
   - 전투 화면이 완전히 보임
   - 배경 방해 없음
   - 카드가 떠있는 느낌

2. **카드 정보 가독성 향상**
   - 카드 이름이 위로 올라가서 card-cost-corner와 겹치지 않음
   - 아이콘 영역이 균형있게 배치
   - 공격력/방어력 정보(card-footer)가 잘 보임

3. **공간 활용 최적화**
   - card-header의 불필요한 여백 제거
   - card-icon-area의 공간 배분 개선
   - 모든 정보가 220px 안에 잘 들어감

4. **시각적 깔끔함**
   - 손패 배경이 없어서 화면이 깔끔
   - 카드 레이아웃이 균형있게 조정
   - 정보 흐름이 자연스러움

### 카드 정보 표시 개선

**이전 문제**:
- card-header가 너무 아래에 있어서 card-icon-area가 card-footer를 밀어냄
- card-footer가 overflow: hidden으로 잘릴 위험
- 공격력 등 중요 정보가 안 보일 수 있음

**해결**:
- card-header를 위로 올려서 공간 확보
- card-icon-area를 flex: 1로 조정
- card-footer가 충분한 공간 확보
- 모든 정보가 잘 보임

## 이전 버전과의 차이

**이전 버전 (v11-16-10:51)**:
- 손패: 50% 투명 (0.5 alpha)
- card-header: margin-top 72px
- card-icon-area: margin-top auto

**현재 버전**:
- 손패: 완전 투명 (transparent)
- card-header: margin-top 8px
- card-icon-area: flex: 1

## 다음 AI를 위한 참고사항

1. **손패 투명도**:
   - 현재 완전 투명 (transparent)
   - 텍스트 가독성 문제 발생 시 약간의 배경 추가 고려
   - 예: background: rgba(0, 0, 0, 0.3)

2. **카드 레이아웃**:
   - card-header margin-top: 8px는 card-cost-corner(top: 16px, height: 42px)와 겹칠 수 있음
   - 필요시 card-header margin-top을 60px 정도로 조정
   - 또는 card-cost-corner 크기 축소

3. **card-icon-area**:
   - flex: 1로 남은 공간을 차지
   - min-height: 60px로 최소 크기 보장
   - 아이콘이 너무 크면 min-height 조정 필요

4. **카드 높이**:
   - 현재 220px 고정
   - 필요시 높이 증가 가능
   - 손패 텍스트를 밑으로 내려도 괜찮다고 했으므로 여유 있음

5. **투명도 복원**:
   - 다시 배경을 추가하려면:
     ```css
     background: rgba(4, 6, 22, 0.5);
     backdrop-filter: blur(8px);
     border: 1px solid rgba(255, 255, 255, 0.08);
     box-shadow: 0 -10px 40px rgba(0, 0, 0, 0.6);
     ```

## 변경 파일 목록

- `src/components/battle/legacy-battle.css`
  - .hand-area 완전 투명화 (524-537줄)
  - .game-card-large .card-header 위치 조정 (465-472줄)
  - .game-card-large .card-icon-area 레이아웃 개선 (474-481줄)
- `src/components/map/MapDemo.jsx`
  - 버전 태그 업데이트
- `CHANGELOG/v11-16-10-57.md`
  - 새 버전 문서 생성
