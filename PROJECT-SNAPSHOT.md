# 프로젝트 스냅샷 (2025-11-15)

> 이 문서는 GPT Codex와 Claude가 동일한 작업 환경을 공유하기 위한 스냅샷입니다.
> 새 작업 세션 시작 시 이 문서를 먼저 확인하세요.

---

## 📦 레포지토리 정보

### 1. 문서 레포: `hahahahgo`

```bash
📍 위치: /home/user/hahahahgo (또는 로컬 경로)
🌿 브랜치: claude/game-development-collab-016AHeBc1gjCpKT5y2DspZE7
📝 최신 커밋: 656ad31 - "Add files via upload"
🔗 원격: http://local_proxy@127.0.0.1:23667/git/haedavja/hahahahgo
```

**파일 구조:**
```
hahahahgo/
├── DEVLOG.md                      # 일일 개발 로그
├── Implementation_Plan.md         # 구현 계획 (4406 bytes)
├── game-progress-md.md           # 진행 현황 (2259 bytes)
├── 통합규칙.md                    # 작업 기본 규칙
├── 에테르.md                      # 에테르 시스템 사양
├── COLLABORATION-GUIDE.md        # 협업 가이드 (NEW)
├── PROJECT-SNAPSHOT.md           # 이 문서 (NEW)
├── sync-workspace.sh             # Linux/Mac 동기화 스크립트 (NEW)
└── sync-workspace.bat            # Windows 동기화 스크립트 (NEW)
```

### 2. 게임 코드 레포: `hahahahgo/new/strategy-map` (로컬에만 존재)

```bash
📍 위치: /home/user/hahahahgo/new/strategy-map
🌿 브랜치: master (추정)
📝 최신 커밋: (Claude 환경에는 아직 없음)
🔗 원격: (로컬 개발자가 푸시 예정)
```

**파일 구조 (DEVLOG 기반 추정):**
```
strategy-map/
├── package.json
├── vite.config.js
├── src/
│   ├── main.jsx
│   ├── components/
│   │   ├── battle/
│   │   │   └── LegacyBattleApp.jsx    # React 전투 UI
│   │   └── map/
│   │       └── MapDemo.jsx             # 맵 UI
│   ├── scenes/                         # Phaser 씬들
│   ├── stores/                         # 상태 관리
│   └── utils/
│       ├── etherCalculator.js          # 에테르 계산
│       └── comboDetector.js            # 콤보 검출
├── public/
│   ├── battle-legacy.html              # 레거시 전투 화면 (참고용)
│   └── assets/
└── dist/                               # 빌드 출력
```

---

## 🔧 실행 환경

### 필수 소프트웨어

| 소프트웨어 | 버전 | 용도 |
|-----------|------|------|
| Node.js | 16.x+ | Vite 개발 서버 |
| npm | 8.x+ | 패키지 관리 |
| Git | 2.x+ | 버전 관리 |

### 실행 명령어

```bash
# 1. 저장소 클론 (처음 한 번만)
git clone <remote-url> hahahahgo
cd hahahahgo

# 2. 게임 코드 클론 (로컬 개발자가 푸시한 후)
cd new
git clone <game-repo-url> strategy-map
cd strategy-map

# 3. 의존성 설치
npm install

# 4. 개발 서버 실행 (✅ 정상 작동)
npm run dev
# → http://localhost:5173

# 5. 프로덕션 빌드 (⚠️ 현재 오류)
npm run build
# → 오류: 종료 코드 -1073740791
```

---

## 🎯 현재 작업 상태

### 완료된 작업 ✅

- [x] 전투 UI를 `LegacyBattleApp.jsx`로 React 이관
- [x] 타임라인, 카드 선택/정렬 시스템 구현
- [x] 콤보 & 에테르 계산 로직 구현
- [x] 적 AI (`decideEnemyMode`, `generateEnemyActions`)
- [x] Vite + React + Phaser 프로젝트 구조 정리
- [x] `npm run dev` 정상 작동 확인

### 진행 중인 이슈 ⚠️

| 이슈 | 우선순위 | 상태 | 세부사항 |
|------|---------|------|----------|
| `npm run build` 실패 | 🔴 High | 조사 중 | Windows 종료 코드 -1073740791<br>`transforming... ✓ 44 modules transformed.` 이후 종료 |
| Phaser 연출 미연동 | 🟡 Medium | 대기 | 현재 React DOM만 사용<br>Phaser Scene/VFX 통합 필요 |
| 테스트 코드 부재 | 🟡 Medium | 계획 | Vitest + React Testing Library 도입 예정 |
| 맵↔전투 흐름 검증 | 🟢 Low | 대기 | `onBattleResult` 콜백 검증 필요 |

### 다음 우선순위 작업

1. **빌드 문제 해결**
   - `npm run build --debug` 로그 수집
   - Windows 권한/Phaser import/이미지 경로 확인
   - DEVLOG에 조사 내용 기록

2. **맵↔전투 통합 검증**
   - 실제 맵 화면에서 전투 진입 테스트
   - `onBattleResult` 콜백 동작 확인
   - Post-combat 선택지 노출 확인

3. **테스트 인프라 구축**
   - `detectPokerCombo()` 단위 테스트
   - `applyPokerBonus()` 단위 테스트
   - `generateEnemyActions()` 단위 테스트

4. **Phaser 연출 통합**
   - `legacy/` 폴더에서 Phaser 샌드박스 실험
   - React UI와 Phaser Scene 공존 구조 설계

---

## 📚 핵심 시스템 사양

### 에테르 시스템 (`에테르.md`)

```javascript
// 에테르 인플레이션 규칙
// 1칸 = 100pt (기본)
// 2칸 = 100 + 150 = 250pt (1칸당 50% 증가)
// 3칸 = 250 + 225 = 475pt
// N칸 = 100 * (1 + 1.5 + 1.5^2 + ... + 1.5^(N-1))

function calculateEtherCost(currentLevel) {
  if (currentLevel === 0) return 100;
  return Math.floor(100 * Math.pow(1.5, currentLevel));
}

// 에테르 칸 표시: x0, x1, x2, ..., x10
// 10칸 → 0칸 복귀 시 다시 100pt부터 시작
```

### 카드 콤보 시스템

| 콤보 | 조건 | 효과 |
|------|------|------|
| 로얄 스트레이트 플러시 | 같은 무늬 A-K-Q-J-10 | 최대 데미지 |
| 스트레이트 플러시 | 같은 무늬 연속 5장 | 매우 큰 데미지 |
| 포카드 | 같은 숫자 4장 | 큰 데미지 |
| 풀하우스 | 3장 + 2장 | 중간 데미지 |
| ... | ... | ... |

---

## 🤝 협업 프로토콜

### 작업 시작 전

```bash
# 1. 동기화
./sync-workspace.sh status
./sync-workspace.sh pull

# 2. 최신 커밋 확인
git log --oneline -3

# 3. 현재 브랜치 확인
git branch --show-current
```

### 작업 중

```markdown
**[작업 선언]**
- 담당: [Claude/Codex/사용자]
- 작업: [구체적 설명]
- 파일: [대상 파일들]
- 예상 시간: [시간]

**[작업 완료]**
- 커밋: [해시] [메시지]
- 변경 파일: [목록]
- 테스트: [검증 방법]
```

### 작업 종료 시

```bash
# 1. 커밋
git add -A
git commit -m "feat: [작업 내용]

- [변경사항 1]
- [변경사항 2]
- [변경사항 3]
"

# 2. 푸시
git push origin <브랜치명>

# 3. DEVLOG 업데이트
# DEVLOG.md에 작업 내용, 이슈, 조치사항 기록
```

---

## 📖 참고 문서 우선순위

| 순서 | 문서 | 용도 |
|------|------|------|
| 1 | `통합규칙.md` | **필수 읽기** - 작업 기본 원칙 |
| 2 | `PROJECT-SNAPSHOT.md` | 현재 상태 파악 (이 문서) |
| 3 | `DEVLOG.md` | 최근 작업 이력 & 이슈 확인 |
| 4 | `game-progress-md.md` | 전체 진행 현황 |
| 5 | `Implementation_Plan.md` | 장기 구현 계획 |
| 6 | `에테르.md` | 에테르 시스템 상세 사양 |
| 7 | `COLLABORATION-GUIDE.md` | 협업 워크플로우 상세 |

---

## 🚨 주의사항

### 절대 하지 말아야 할 것

- ❌ 기존 인터페이스를 임의로 변경
- ❌ 별도 지시 없이 내부 디자인 수정
- ❌ `통합규칙.md` 원칙 위반
- ❌ 테스트 없이 핵심 로직 변경
- ❌ DEVLOG 업데이트 없이 작업

### 필수로 해야 할 것

- ✅ 작업 전 `git pull`로 동기화
- ✅ 작업 후 `DEVLOG.md` 업데이트
- ✅ 변경사항 커밋 & 푸시
- ✅ 문제 발생 시 원인 파악 후 기록
- ✅ 다른 AI와 작업 영역 조율

---

## 📞 긴급 연락

### 빌드 실패 시

1. `npm run build --debug` 실행
2. 전체 로그를 `DEVLOG.md`에 첨부
3. 의심 지점 (Phaser import, 이미지 경로 등) 기록
4. 필요시 `npm run build 2>&1 | tee build.log` 로그 저장

### Git 충돌 시

1. `git status`로 충돌 파일 확인
2. 수동으로 충돌 해결
3. `git add [파일]`
4. `git commit -m "merge: Resolve conflict in [파일명]"`
5. `git push`

### 레거시 코드 복구 필요 시

- `public/battle-legacy.html`은 백업용
- 절대 수정하지 말고 참조만 할 것
- 신규 기능은 `LegacyBattleApp.jsx`에만 추가

---

## 🔄 업데이트 이력

| 날짜 | 버전 | 변경사항 |
|------|------|----------|
| 2025-11-15 | 1.0.0 | 초기 스냅샷 생성 |

---

**다음 스냅샷 업데이트 시점:**
- 프로젝트 구조 변경 시
- 새 레포지토리 추가 시
- 빌드 문제 해결 시
- 주요 마일스톤 달성 시
